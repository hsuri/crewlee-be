steps:
  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'
      - '.'

  # Push to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest']

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - '${_SERVICE_NAME}'
      - '--image=gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'
      - '--region=${_REGION}'
      - '--platform=managed'
      - '--add-cloudsql-instances=${_CLOUD_SQL_INSTANCE}'
      - >-
        --set-env-vars=ENVIRONMENT=production,DATABASE_URL=${_DATABASE_URL},DB_PASSWORD=${_DB_PASSWORD},ADMIN_PASSWORD=${_ADMIN_PASSWORD},ALLOWED_ORIGINS=${_ALLOWED_ORIGINS},VOYAGE_API_KEY=${_VOYAGE_API_KEY},ANTHROPIC_API_KEY=${_ANTHROPIC_API_KEY},RAG_BUCKET_NAME=${_RAG_BUCKET_NAME}

substitutions:
  _REGION: us-central1
  _SERVICE_NAME: crewlee-api
  _CLOUD_SQL_INSTANCE: ''
  _DATABASE_URL: ''
  _DB_PASSWORD: ''
  _ADMIN_PASSWORD: admin123
  _ALLOWED_ORIGINS: 'http://localhost:3000'
  _VOYAGE_API_KEY: ''
  _ANTHROPIC_API_KEY: ''
  _RAG_BUCKET_NAME: crewlee-rag-docs

images:
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'
